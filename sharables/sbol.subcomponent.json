{
  "schema": "clotho.sharable/1",
  "id": "sbol.subcomponent",
  "name": "SBOL SubComponent",
  "type": "schema",
  "description": "one component appearing inside another — the part/sub-part relationship, and where it sits",
  "phrases": [
    "what a subcomponent is",
    "sbol subcomponent",
    "a part inside a plasmid",
    "instance_of and locations"
  ],
  "tags": [
    "sbol",
    "ontology"
  ],
  "noun": "subcomponent",
  "verb": "define",
  "notes": "Taken from the SBOL 3 data model, with SBOL's own class and property names — docs/ONTOLOGY.md § Alignment with SBOL. Where this departs from SBOL it says so in this field; silence means it does not depart. Not every SBOL property is modelled yet: what is here is what this lab's work needs, and adding one is adding a field, not a redesign.",
  "definition": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "SBOL SubComponent",
    "type": "object",
    "required": [
      "instance_of"
    ],
    "additionalProperties": true,
    "properties": {
      "instance_of": {
        "type": "string",
        "description": "the sbol.component this is an instance of"
      },
      "locations": {
        "type": "array",
        "items": {
          "type": "object"
        },
        "description": "where in the parent's sequence it sits"
      },
      "measures": {
        "type": "array",
        "items": {
          "type": "object"
        }
      },
      "name": {
        "type": "string"
      }
    }
  },
  "created": "2026-09-11",
  "updated": "2026-09-11"
}
